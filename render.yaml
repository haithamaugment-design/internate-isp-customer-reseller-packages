services:
  - type: web
    name: netmaster
    runtime: node
    buildCommand: npm install && npm run render-build
    startCommand: npx tsx apps/web/server.ts
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
